---
sidebar_position: 1
title: clearTimeout
---
import Link from '@docusaurus/Link';

> **<Link to="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function">function</Link>**

Exported method to clear BeAPI intervals.

:::caution

Intervals and timeouts need a custom implementation due to how gametest works. You must use them instead of the default JS implementations.

:::

## Method

```js
clearInterval(timeout);
```

| PARAMETER | TYPE | OPTIONAL | DEFAULT | DESCRIPTION |
| :-------: | :--: | :------: | :-----: | :---------: |
| timeout | <Link to="/docs/typedefs/interval">Interval</Link> | ❌ | `null` | Interval object. |

## Returns
void

## Examples

```js
import {
  clearTimeout,
  setTimeout,
} from 'beapi-core';

// Create a timeout for 40 ticks
const timeout = setTimeout(() => console.warn('Timeout ran!'), 40)

// Create a max value
const max = 10
// Generate a random number between 0 and 10
const random = Math.round(Math.random() * max)

// If the random number happens to be 5 stop the timeout
if (random === 5) clearTimeout(timeout)
```
export enum AgentDirection {
  forward = 'forward',
  back = 'back',
  up = 'up',
  down = 'down',
  left = 'left',
  right = 'right',
}

export enum AgentRotation {
  left = 'left',
  right = 'right',
}

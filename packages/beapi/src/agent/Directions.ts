/**
 * Agent direction enumable.
 */
export enum AgentDirection {
  forward = 'forward',
  back = 'back',
  up = 'up',
  down = 'down',
  left = 'left',
  right = 'right',
}

/**
 * Agent rotation enumable.
 */
export enum AgentRotation {
  left = 'left',
  right = 'right',
}

import { Commands } from "Minecraft"

class DatabaseManager {

    /**
     * 
     * @param {any} main Passes the other functions from api.js
     */

    constructor(main) {
        this.main = main
    }

    /**
     * 
     * @function onEnabled() Runs when the Database Manager is enabled
     * @returns
     */

    onEnabled() {
        return
    }
}

export default DatabaseManager